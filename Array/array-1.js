const array=['alex','john','piter'];
console.log(array);
console.log('first element',array[0]);
console.log('last element',array[2]);
for (let i = 0; i < array.length; i++) {
    const element = array [i];
    console.log(`${i}`,element);
}